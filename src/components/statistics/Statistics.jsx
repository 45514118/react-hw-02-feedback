export const Statistics = ({good, neutral, bad}) => {
    return (
        <>
            Statistics....OK!
            <ul>
                <li key='1'>{good}</li>
                <li key='2'>{neutral}</li>
                <li key='3'>{bad}</li>
            </ul>
        </>
    )
}
